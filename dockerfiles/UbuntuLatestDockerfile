# Base Image
# Dockerfile for a Ubuntu:latest image

# Pull latest Ubuntu image
FROM ubuntu:latest

# install software packages
RUN apt-get update && apt-get -y upgrade && apt-get -y install iputils-ping iproute2 net-tools dnsutils mtr-tiny nano traceroute
