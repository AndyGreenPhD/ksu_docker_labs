# Base Image
# Dockerfile for a Kali:latest image

# Pull latest Ubuntu image
FROM kalilinux/kali-rolling

# install software packages
RUN apt update && apt -y install iputils-ping iproute2 net-tools dnsutils mtr-tiny nano traceroute
